<form action="/booking" method="POST">
  <button
    type="submit"
    class="bg-gradient-to-r from-purple-500 to-pink-500 hover:from-purple-700 hover:to-pink-700 active:scale-95 transition-all duration-200 text-white px-2 py-1 rounded-xl shadow-lg w-full font-bold text-base tracking-wide"
  >
    Book now
  </button>
  <input type="hidden" name="id" value="<%= home._id %>" />
</form>
