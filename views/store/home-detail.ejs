<%- include('../partials/head') %>
</head>
<body class="bg-gradient-to-br from-purple-300 via-pink-200 to-purple-100 min-h-screen">
    <%- include('../partials/nav') %>
    <main class="container mx-auto bg-white/80 shadow-2xl p-4 sm:p-6 md:p-8 mt-6 sm:mt-10 max-w-full md:max-w-4xl rounded-3xl">
        <h2 class="text-center font-bold mb-6 text-purple-600 text-2xl sm:text-3xl tracking-wide">
            <span class="inline-block align-middle">🏡</span> <%= home.houseName %>
        </h2>
        <div class="flex flex-col md:flex-row gap-8">
            <div class="md:w-1/2 w-full flex-shrink-0">
                <div class="rounded-2xl overflow-hidden shadow-lg border-4 border-purple-200 bg-gradient-to-br from-purple-100 via-pink-100 to-purple-50">
                    <img src="<%= home.photo %>" alt="<%= home.houseName %>" class="w-full h-56 sm:h-72 object-cover group-hover:scale-105 transition-transform duration-300">
                </div>
            </div>
            <div class="md:w-1/2 w-full flex flex-col gap-6 justify-between">
                <div>
                    <h3 class="text-lg sm:text-xl font-semibold text-purple-800 mb-1">Description</h3>
                    <p class="text-purple-700 text-base sm:text-lg mb-3"><%= home.description %></p>
                    <div class="flex flex-wrap gap-4">
                        <div class="flex items-center gap-2 bg-purple-50 rounded-lg px-3 py-2 shadow">
                            <span class="text-purple-600 text-lg">📍</span>
                            <span class="text-purple-700 font-medium"><%= home.location %></span>
                        </div>
                        <div class="flex items-center gap-2 bg-pink-50 rounded-lg px-3 py-2 shadow">
                            <span class="text-purple-600 text-lg">💰</span>
                            <span class="text-purple-700 font-semibold">₹<%= home.price %> <span class="text-xs font-normal">/ night</span></span>
                        </div>
                        <div class="flex items-center gap-2 bg-yellow-50 rounded-lg px-3 py-2 shadow">
                            <span class="text-yellow-500 text-lg">⭐</span>
                            <span class="text-purple-700 font-medium"><%= home.rating %> / 5</span>
                        </div>
                    </div>
                </div>
                <div class="flex flex-col gap-3">
                    <div>
                        <%- include('../partials/favourite',{home:home}) %>
                    </div>
                </div>
            </div>
        </div>
    </main>
    <%- include('../partials/footer') %>
</body>
</html>
